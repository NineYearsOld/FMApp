<Window x:Class="UI.ZoekWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="ZoekWindow" Height="500" Width="800"
        Loaded="Window_Loaded"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock Name="error" HorizontalAlignment="Center" VerticalAlignment="Top"/>
        <Frame Name="zoek" Grid.ColumnSpan="2"/>
    <Button Name="bevestig" Content="Bevestig" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,5,65" Width="100" Height="25" Click="bevestig_Click"/>
    </Grid>
</Window>
